<Application
    x:Class="SmartSearch.Desktop.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
      </ResourceDictionary.MergedDictionaries>
      <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="MinHeight" Value="36" />
      </Style>
      <Style TargetType="ComboBox" BasedOn="{StaticResource DefaultComboBoxStyle}">
        <Setter Property="MinHeight" Value="36" />
      </Style>
      <Style TargetType="TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
        <Setter Property="MinHeight" Value="36" />
      </Style>
      <Style TargetType="PasswordBox" BasedOn="{StaticResource DefaultPasswordBoxStyle}">
        <Setter Property="MinHeight" Value="36" />
      </Style>
      <Style x:Key="PageHeadingStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="26" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
      </Style>
      <Style x:Key="SectionHeadingStyle" TargetType="TextBlock" BasedOn="{StaticResource PageHeadingStyle}">
        <Setter Property="FontSize" Value="18" />
      </Style>
      <Style x:Key="BodyCopyStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
      </Style>
      <Style x:Key="SecondaryCopyStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyCopyStyle}">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
      </Style>
      <Style x:Key="DataCopyStyle" TargetType="TextBlock" BasedOn="{StaticResource SecondaryCopyStyle}">
        <Setter Property="FontFamily" Value="Consolas" />
        <Setter Property="IsTextSelectionEnabled" Value="True" />
      </Style>
      <Thickness x:Key="SurfaceCardPadding">16</Thickness>
      <Style x:Key="SurfaceCardStyle" TargetType="ContentControl">
        <Setter Property="Padding" Value="{StaticResource SurfaceCardPadding}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ContentControl">
              <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1" CornerRadius="10" Padding="{TemplateBinding Padding}">
                <ContentPresenter Content="{TemplateBinding Content}"
                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                  Padding="0"
                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="PaneDividerStyle" TargetType="Thumb">
        <Setter Property="Width" Value="8" />
        <Setter Property="IsTabStop" Value="True" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Thumb">
              <Grid Background="Transparent">
                <Border Width="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="SectionDividerStyle" TargetType="Border">
        <Setter Property="Height" Value="1" />
        <Setter Property="Background" Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
      </Style>
      <Style x:Key="WorkspaceFooterStyle" TargetType="Border">
        <Setter Property="Padding" Value="24,12" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
      </Style>
      <Style x:Key="WorkspaceToolbarStyle" TargetType="Border">
        <Setter Property="Padding" Value="24" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
      </Style>
      <Style x:Key="ToolbarIconButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="36" />
        <Setter Property="MinWidth" Value="36" />
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
      </Style>
      <Style x:Key="FieldHelpButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="Width" Value="24" />
        <Setter Property="Height" Value="24" />
        <Setter Property="MinWidth" Value="24" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
        <Setter Property="CornerRadius" Value="12" />
      </Style>
      <SolidColorBrush x:Key="ConnectionReadyBrush" Color="#35C759" />
      <Style x:Key="ConnectionIdleStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="8" />
        <Setter Property="Height" Value="8" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Fill" Value="{ThemeResource TextFillColorSecondaryBrush}" />
      </Style>
      <Style x:Key="ConnectionReadyStyle" TargetType="Ellipse" BasedOn="{StaticResource ConnectionIdleStyle}">
        <Setter Property="Fill" Value="{StaticResource ConnectionReadyBrush}" />
      </Style>
      <Style x:Key="ConnectionPendingStyle" TargetType="Ellipse" BasedOn="{StaticResource ConnectionIdleStyle}">
        <Setter Property="Fill" Value="{ThemeResource SystemFillColorCautionBrush}" />
      </Style>
      <Style x:Key="ConnectionFailedStyle" TargetType="Ellipse" BasedOn="{StaticResource ConnectionIdleStyle}">
        <Setter Property="Fill" Value="{ThemeResource SystemFillColorCriticalBrush}" />
      </Style>
      <Style x:Key="StatusNeutralStyle" TargetType="ContentControl">
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ContentControl">
              <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                <ContentPresenter Content="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="StatusSuccessStyle" TargetType="ContentControl" BasedOn="{StaticResource StatusNeutralStyle}">
        <Setter Property="Background" Value="{ThemeResource SystemFillColorSuccessBackgroundBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource SystemFillColorSuccessBrush}" />
      </Style>
      <Style x:Key="StatusWarningStyle" TargetType="ContentControl" BasedOn="{StaticResource StatusNeutralStyle}">
        <Setter Property="Background" Value="{ThemeResource SystemFillColorCautionBackgroundBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource SystemFillColorCautionBrush}" />
      </Style>
      <Style x:Key="StatusErrorStyle" TargetType="ContentControl" BasedOn="{StaticResource StatusNeutralStyle}">
        <Setter Property="Background" Value="{ThemeResource SystemFillColorCriticalBackgroundBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource SystemFillColorCriticalBrush}" />
      </Style>
      <Style x:Key="StatusActiveStyle" TargetType="ContentControl" BasedOn="{StaticResource StatusNeutralStyle}">
        <Setter Property="Background" Value="{ThemeResource AccentFillColorTertiaryBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
