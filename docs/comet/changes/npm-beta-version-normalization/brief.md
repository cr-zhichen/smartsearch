# 目标

修复 PR60/61 合入后自动 beta 发布因 npm 与 Python 预发布版本表示不同而中止的问题，恢复已有 next/beta 发布链。

# 范围

统一打包前已安装 Python 元数据与源码版本的语义比较；沿用构建环境已有 packaging 库。补充覆盖稳定、alpha、beta、rc、dev 和真实版本差异的回归，验证真实 beta 冻结运行时、npm 实包、六平台 CI 和已有自动发布。

故障证据：main 2379557ec3e14a81c977275d8a54b86c0a10bc3e 的发布 run 35710112732 将版本设为 0.1.24-beta.2，安装元数据规范化为 0.1.24b2，build_backend.py 的原始字符串比较误报 stale。源码来源为当前已合并 PR60/61；本轮无需重新设计其产品行为。

# 非目标

不继续 JEV 路由改造，不改 App/CLI/Skills 产品功能，不重打旧稳定 tag，不切换 npm latest，不覆盖个人安装、配置、密钥或未提交工作。

# 验收示例

以 specs/npm-binary-version/spec.md 的三个 Scenario 为准，分别验证等价版本、真实版本错误、实际 beta 分发链。

# 约束与不变量

在独立 worktree codex/npm-beta-version-normalization 中基于 main 2379557 修复。保留真正装错版本时拒绝打包的保护；对外 npm/CLI/协议版本仍保持 npm 原始 SemVer，不能仅绕过检查。复用既有构建依赖，不新增运行时版本框架。遵守六平台先构建后发布平台包、最后发布主包的顺序。

# 决策

用户在已说明具体故障后明确要求“帮我解决一下”，已授权修复该既有行为并恢复其 beta 发布；本次是范围明确的缺陷修复，无新的产品取舍。沿用其前一轮主分支合并授权，验证后将必要修复合入 main，触发现有 next 发布；稳定版 latest 和桌面正式发布不在本轮范围。

最小实现采用 packaging.version.Version 比较安装元数据和源码声明，npm 原始版本标识保持不变。先留下能在原实现上失败的回归，再修复并做真实 beta 实包验证。单 change 处理，无 Supervisor 拆分。

# 待解决问题

无待决产品问题；若实际发布被外部 npm 权限阻断，只报告真实所需权限，不读取或修改用户密钥。

# 验证预期

本地回归证明原 beta 失败可复现、修复后等价版本通过而错误版本仍失败；实包验证自带运行时、协议与公开版本一致；六平台 CI 及发布读回使用修复后的提交。正式只读 Verifier 独立覆盖三个验收场景。个人记忆不写入任务细节。
